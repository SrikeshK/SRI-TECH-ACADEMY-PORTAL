rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full access to all collections for authenticated users
    // For admin/internal portal - allow all read/write for authenticated users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
